{
  "name": "cookie",
  "private": true,
  "scripts": {
    "postinstall": "cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build && mkdir -p ../server/public && cp -r dist/* ../server/public/",
    "start": "cd server && npm run db:migrate && npm run db:seed && npm start"
  }
}
